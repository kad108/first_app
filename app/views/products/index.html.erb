<p id="notice"><%= notice %></p>

<h1>Products</h1>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image</th>
      <th>Colour</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td width=10%><%= product.name %></td>
        <td width=33%><%= product.description %></td>
        <td><%= image_tag(product.image_url, class: "img-responsive") %></td>
        <td width=25%><%= product.colour %></td>
        
      </tr>
      <tr>
        <td><%= link_to 'Show', product %>&nbsp;&#124;&nbsp;<%= link_to 'Edit', edit_product_path(product) %>&nbsp;&#124;&nbsp;<%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
 </table>
 -->

<div class="container">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <% @products.each do |product| %>
        <div class="col-sm-2 col-md-4 well">
        <b><h3><%= product.name %></h3></b>
        <div><%= image_tag(product.image_url, class: "img-responsive caption center-block") %>
        </div>
        <div>
          <%= product.description %>
        </div>
        <div>
          <hr class="end_product">
          Price: Ł<%= product.price %>
        </div>
        <%= link_to 'Show', product %>&nbsp;&#124;&nbsp;<%= link_to 'Edit', edit_product_path(product) %>&nbsp;&#124;&nbsp;<%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
        </div>
      <% end %>
    </div>
  </div>
</div>


<br>

<%= link_to 'New Product', new_product_path %>

<div class="main"></div>